apiVersion: v1
kind: Service
metadata:
  name: versioning-service
  namespace: cse474-prod
spec:
  type: ClusterIP
  selector:
    app: versioning-service
  ports:
    - port: 3000
      targetPort: 3000