apiVersion: v1
kind: Service
metadata:
  name: versioning-service
  namespace: cse474-prod
spec:
  type: ClusterIP
  selector:
    app: versioning-service
ports:
  - name: http
    port: 3000
    targetPort: 3000